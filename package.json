{
  "name": "herdr-picker",
  "version": "0.1.1",
  "description": "Pick a DOM element on any page in Chrome and send it, with a prompt, to a coding agent running in herdr.",
  "type": "module",
  "author": {
    "name": "Marco Boffo",
    "email": "chat@marcoboffo.me"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scaccogatto/herdr-picker.git"
  },
  "homepage": "https://github.com/scaccogatto/herdr-picker#readme",
  "bugs": {
    "url": "https://github.com/scaccogatto/herdr-picker/issues"
  },
  "keywords": [
    "chrome-extension",
    "herdr",
    "claude-code",
    "coding-agent",
    "dom",
    "element-picker",
    "native-messaging",
    "devtools",
    "ai"
  ],
  "bin": {
    "herdr-picker": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vite build && vite build --mode extension",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "oxlint",
    "e2e": "playwright test",
    "store-assets": "STORE_ASSETS=1 playwright test e2e/store-assets.spec.ts",
    "prepublishOnly": "npm run typecheck && npm run test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.3.0",
    "@types/node": "^26.4.1",
    "@vitest/coverage-v8": "^5.0.0",
    "jsdom": "^30.0.1",
    "oxlint": "^1.83.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
